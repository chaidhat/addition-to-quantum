A physical [[wave-function]] $\phi(x)$ has these properties:

1. As x goes to infinity, the $\phi(x)$ goes to zero. It is normalized.
2. $\phi(x)$ is continuous
3. $\frac{d\phi(x)}{dx}$ is continuous UNLESS potential energy $V(x)$ is infinite

